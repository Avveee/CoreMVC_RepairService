@model CoreMVC_Exam.ViewModels.AdministrationFormViewModel

@{
    ViewBag.Title = "Users";
}

@*<h3>Roles</h3>

<table id="roles" class="dataTable">
    <thead>
        <tr>
            <th>Role Id</th>
            <th>Role name</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model.Roles)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>
                <td>@Html.ActionLink("Delete", "DeleteRole", "Administration", new { @id = role.Id }, null)</td>
            </tr>
        }
    </tbody>
</table>*@

<a class="create-button" asp-page="/Account/Register" asp-area="Identity">New user</a>

<table id="users" class="dataTable">
    <thead>
        <tr>
            <th>User Id</th>
            <th>User name</th>
            <th>Email</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>@Html.ActionLink("Delete", "Delete", "Administration", new { @id = user.Id }, null)</td>
            </tr>
        }
    </tbody>
</table>